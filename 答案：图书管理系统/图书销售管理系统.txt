销售管理：为SaleRecord添加销售数量，价格，书记录等做处理
获取所有SR：得到关联BSR，利用BSR为SR其他成员变量赋值/S
模糊查询：根据日期得到SR，赋值
插入或者更新BSR（参数为引用类型）
-------------------------
书管理：实现类与接口，结构与出版社差不多
获取全部书籍：根据外键id设置相应的引用（S实现关联/三个接口）
模糊查询：同理
查看书：同理，显示所有种类，根据种类查找书集合（外键查询/
	将外键转化为id后id查询）
新增书本：D层根据外键名（先赋值再转换）获取id（或者
	新增出版社和种类/直接调用本包下其他接口），记得无参构造器
根据集合得到单体
-----------------------
出版社管理
设计父类：得到JDBCExecutor，根据sql语句数据集合
设计出版社访问层D，业务层S接口（每个表都有）
获取全部出版社D类：继承父类实现接口，接口中添加相应方法，
	直接写sql语句，调用父类方法查找并返回数据
S类：通过D类对象，得到数据（表现层处理：
	将数据转换为视图格式/两个集合，
	所有出版社，与单个出版社信息）
模糊查询：相当于关键字查询，刷新
查看出版社：相当于id查询，表现层监听得到id，调用业务层接口得到ValueObject
	（sql查询），显示信息
新增出版社：表现层得到内容，转换成对象，sql拼接实现新增（返回主键）
修改出版社：同理
------------------------
数据表设计
设计数据表类：公用字段在父类，
	然后设计实体类（包含数据表中所有字段/全为String类型）
读取配置文件类（算是个工具类/一般为静态方法）
jdbc操作类：获取conn，stmt，conn成本大，新建单例类来维护conn
	传入sql语句然后执行，插入返回主键（通过stmt获取），否则-1
数据转换工具类：
	将结果集封装成集合（反射/传入实体类class）：
		新建该实体类对象vo
		成员变量数组相加（转化为List相加再转化为数组）
		得到实体类中setter方法名（字符串拼接）
		得到字段内容，并使用vo对象setter设置字段值



